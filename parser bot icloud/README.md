# Parser Bot iCloud

Основной проект лежит в подпапке `competitor_price_monitor`.

Запуск Telegram-бота:

```bash
cd "/Users/pavelisaenko/Documents/New project/parser bot icloud"
./run_telegram_bot.sh
```

Рабочие настройки находятся здесь:

```text
competitor_price_monitor/.env
```

Python-пакет внутри специально оставлен с именем `competitor_price_monitor`, потому что имя пакета с пробелами сломало бы импорты.
